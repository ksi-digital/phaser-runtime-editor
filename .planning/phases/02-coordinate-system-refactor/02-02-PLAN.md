---
phase: 02-coordinate-system-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/EditorScene.ts
  - src/gizmos/GizmoManager.ts
  - src/gizmos/MoveGizmo.ts
  - src/gizmos/RotateGizmo.ts
  - src/gizmos/ScaleGizmo.ts
  - src/gizmos/HitAreaGizmo.ts
  - src/core/SelectionManager.ts
  - src/core/SnappingEngine.ts
  - src/ui/InspectorPanel.ts
  - src/ui/EditorUI.ts
autonomous: true
requirements:
  - COORD-01
  - COORD-03
  - COORD-05

must_haves:
  truths:
    - "EditorScene.update() captures ViewportState once per frame and passes it to all subsystems"
    - "All 4 gizmos store a ViewportState snapshot at drag start and use it throughout the drag"
    - "MoveGizmo caches the inverted parent matrix at drag start instead of recomputing per frame"
    - "Hit-area transform duplicates in EditorScene, SelectionManager, and HitAreaGizmo are replaced with CoordinateSystem helper calls"
    - "No Phaser.Scene is passed to any CoordinateSystem method anywhere in the codebase"
    - "InspectorPanel and SnappingEngine accept ViewportState instead of Phaser.Scene"
    - "The project compiles successfully with zero TypeScript errors"
  artifacts:
    - path: "src/EditorScene.ts"
      provides: "Per-frame ViewportState capture and distribution"
      contains: "captureViewport"
    - path: "src/gizmos/MoveGizmo.ts"
      provides: "Frozen ViewportState at drag start + cached inverse parent matrix"
      contains: "cachedInvParentMatrix"
    - path: "src/gizmos/HitAreaGizmo.ts"
      provides: "Uses CoordinateSystem.getHitAreaToScreen instead of local duplicate"
      contains: "getHitAreaToScreen"
  key_links:
    - from: "src/EditorScene.ts"
      to: "src/core/ViewportState.ts"
      via: "captureViewport() call in update()"
      pattern: "captureViewport"
    - from: "src/EditorScene.ts"
      to: "src/core/SelectionManager.ts"
      via: "drawSelection(gfx, vp) with ViewportState"
      pattern: "drawSelection.*vp"
    - from: "src/gizmos/MoveGizmo.ts"
      to: "src/core/ViewportState.ts"
      via: "ViewportState stored at drag start"
      pattern: "ViewportState"
    - from: "src/gizmos/GizmoManager.ts"
      to: "src/gizmos/MoveGizmo.ts"
      via: "startDrag passes ViewportState instead of hostScene"
      pattern: "startDrag.*vp"
    - from: "src/gizmos/GizmoManager.ts"
      to: "src/gizmos/RotateGizmo.ts"
      via: "draw() passes ViewportState for getScreenPosition calls in draw/computeRingRadius"
      pattern: "rotateGizmo\\.draw.*vp"
---

<objective>
Thread ViewportState through all consumers: EditorScene captures it per-frame, gizmos freeze it at drag start, SelectionManager/SnappingEngine/InspectorPanel use it for coordinate math. Replace all hit-area transform duplicates. Cache parent matrix inversion in MoveGizmo.

Purpose: Complete the ViewportState migration so the entire codebase uses stable coordinate snapshots. Eliminate per-frame matrix re-inversions. Remove 3 duplicate hit-area transform implementations.

Output: All files compile. No Phaser.Scene passes to CoordinateSystem anywhere. Coordinate math uses ViewportState everywhere.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coordinate-system-refactor/02-RESEARCH.md
@.planning/phases/02-coordinate-system-refactor/02-01-SUMMARY.md

@src/EditorScene.ts
@src/gizmos/GizmoManager.ts
@src/gizmos/MoveGizmo.ts
@src/gizmos/RotateGizmo.ts
@src/gizmos/ScaleGizmo.ts
@src/gizmos/HitAreaGizmo.ts
@src/core/SelectionManager.ts
@src/core/SnappingEngine.ts
@src/ui/InspectorPanel.ts
@src/ui/EditorUI.ts
@src/core/CoordinateSystem.ts
@src/core/ViewportState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread ViewportState through EditorScene, SelectionManager, SnappingEngine, and UI</name>
  <files>src/EditorScene.ts, src/core/SelectionManager.ts, src/core/SnappingEngine.ts, src/ui/InspectorPanel.ts, src/ui/EditorUI.ts</files>
  <action>
**EditorScene.ts:**
- Import `captureViewport` and `ViewportState` from `../core/ViewportState`
- In `update()`: capture ViewportState once at the top: `const vp = captureViewport(this.designWidth, this.designHeight, hostScene, this)` (where hostScene = `this.getHostScene()`). If no hostScene, skip coordinate work.
- Pass `vp` to all callers:
  - `this.selectionMgr.drawSelection(this.gfx, vp)` — add `vp` parameter
  - `this.drawHitArea(this.gfx, sel)` — this uses `getWorldTransformMatrix()` directly (already correct for rendering), but replace the inline `toScreen` closure with `this.coordSystem.getHitAreaToScreen(sel)`. No ViewportState needed here (rendering uses matrix directly).
  - `this.snappingEngine.drawGuides(this.gfx, this.gizmoMgr.snapGuides, this.coordSystem, vp)` — replace `hostScene` with `vp`
  - `this.editorUI.refresh(vp)` — add `vp` parameter
  - `this.updateCoordBar(vp)` — add `vp` parameter
- In `updateCoordBar(vp)`: replace `this.coordSystem.screenToDesign(x, y, hostScene)` with `this.coordSystem.screenToDesign(x, y, vp)`. Replace `getWorldPosition` with `getScreenPosition(sel, vp)`. Replace `getDesignPosition(sel, hostScene)` with `getDesignPosition(sel, vp)`.
- In `drawDesignBounds()`: either accept `vp` parameter and use `vp.scaleFactor`/`vp.offsetX`/`vp.offsetY`, or keep computing locally (it only uses canvas dimensions, not game camera). Prefer accepting `vp` for consistency.
- In the `overlay.on('pointerdown')` handler: capture a ViewportState for the selection log: `const vp = captureViewport(...)`. Replace `this.coordSystem.getDesignPosition(hit, this.getHostScene()!)` with `this.coordSystem.getDesignPosition(hit, vp)`.
- In `create()`: the `drawDesignBounds()` call during create can compute its own snapshot or use local math since it runs once.

**SelectionManager.ts:**
- `drawSelection(gfx, vp?: ViewportState)` — make vp optional initially to not break the build, but use it when provided. Actually, since Plan 01 already changed CoordinateSystem signatures, just make it required: `drawSelection(gfx: Graphics, vp: ViewportState): void`
- In `getChildWorldBounds()`: the fallback calls `this.coords.getWorldPosition(child)` — replace with `this.coords.getScreenPosition(child, vp)`. This means `getChildWorldBounds` needs `vp` parameter, which means `getContainerBounds` needs it, which means `getScreenBounds` needs it.
- Update `getScreenBounds(obj, vp: ViewportState)` signature to accept ViewportState. Pass to `getContainerBounds` and `getPolygonShapeBounds`.
- In `getPolygonShapeBounds()`: replace the inline matrix transform (lines 140-148) with `const toScreen = this.coords.getHitAreaToScreen(poly)`. Then for each point: `const s = toScreen(p.x, p.y)` to get screen coords. This eliminates the duplicate.
- `hitTest(screenX, screenY, vp: ViewportState)` — add vp parameter, pass to `getScreenBounds`.
- Import ViewportState.

**SnappingEngine.ts:**
- `objectSnap(point, allObjects, threshold, coords, vp: ViewportState, excludeObj?)` — replace `hostScene: Phaser.Scene` with `vp: ViewportState`
- `applySnapping(point, config, allObjects, coords, vp: ViewportState, excludeObj?)` — same replacement
- `drawGuides(gfx, guides, coords, vp: ViewportState)` — replace `hostScene` with `vp`. Use `coords.designToScreen(x, y, vp)`.
- Import ViewportState.

**InspectorPanel.ts:**
- Replace `private hostScene: Phaser.Scene | null` with `private vp: ViewportState | null`
- `bind(obj, vp: ViewportState)` — store `this.vp = vp` instead of hostScene
- `refresh(vp?: ViewportState)` — if vp provided, update `this.vp = vp`. The current `refresh()` calls `syncFromObject()` which reads `getDesignPosition(obj, hostScene)` — change to `getDesignPosition(obj, this.vp!)`.
- `applyTransform()`: change `setDesignPosition(target, x, y, hostScene)` to `setDesignPosition(target, x, y, this.vp!)`.
- Import ViewportState.

**EditorUI.ts:**
- Constructor: replace `hostScene: Phaser.Scene` with capturing ViewportState dynamically — or change `refresh()` to accept `vp: ViewportState`.
- `refresh(vp: ViewportState)` — pass `vp` to `this.inspector.bind(obj, vp)` or `this.inspector.refresh(vp)`.
- Wherever hostScene was passed to inspector.bind(), pass vp instead.
- Import ViewportState.
  </action>
  <verify>
`npx tsc --noEmit 2>&1 | grep -c "error TS"` — should show 0 errors for these files. If gizmo files still have errors, that's expected (Task 2 fixes those). Run `npx tsc --noEmit 2>&1` and confirm any remaining errors are ONLY in gizmo files.
  </verify>
  <done>
EditorScene captures ViewportState once per update() and passes to all subsystems. SelectionManager, SnappingEngine, and InspectorPanel accept ViewportState instead of Phaser.Scene. Hit-area duplicate in SelectionManager.getPolygonShapeBounds() replaced with CoordinateSystem.getHitAreaToScreen() call. Hit-area duplicate in EditorScene.drawHitArea() replaced similarly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Thread ViewportState through all gizmos, cache MoveGizmo inverse matrix, replace HitAreaGizmo duplicate</name>
  <files>src/gizmos/GizmoManager.ts, src/gizmos/MoveGizmo.ts, src/gizmos/RotateGizmo.ts, src/gizmos/ScaleGizmo.ts, src/gizmos/HitAreaGizmo.ts</files>
  <action>
**GizmoManager.ts:**
- In `handlePointerDown()`: instead of passing `hostScene` to `startDrag()`, capture a ViewportState snapshot and pass that:
  ```
  const vp = captureViewport(this.coords.designWidth, this.coords.designHeight, hostScene, editorScene);
  ```
  Wait — GizmoManager doesn't have `editorScene` directly. It has `this.game` and `this.hostSceneKey`. Check GizmoManager constructor — it receives `editorScene: Phaser.Scene`. Store it as `this.editorScene`.
- Actually, re-read GizmoManager: it already receives `editorScene` in the constructor (line 34). Store it as a field: `private editorScene: Phaser.Scene`.
- In `handlePointerDown()`: capture `const vp = captureViewport(this.coords.designWidth, this.coords.designHeight, hostScene, this.editorScene)`. Pass `vp` to all `startDrag()` calls instead of `hostScene`.
- Import `captureViewport`, `ViewportState` from `../core/ViewportState`.

**MoveGizmo.ts (COORD-03 + COORD-05):**
- Replace `private hostScene: Phaser.Scene | null` with `private vp: ViewportState | null`
- Add `private cachedInvParentMatrix: { a: number; b: number; c: number; d: number; tx: number; ty: number } | null = null`
- `startDrag(handle, screenX, screenY, target, vp: ViewportState)` — store `this.vp = vp` (frozen snapshot at drag start). Replace `this.coords.getDesignPosition(target, hostScene)` with `this.coords.getDesignPosition(target, vp)`.
- In `startDrag()`, cache the inverse parent matrix:
  ```
  this.cachedInvParentMatrix = null;
  if ('parentContainer' in target && (target as any).parentContainer) {
      const parent = (target as any).parentContainer;
      const m = parent.getWorldTransformMatrix();
      const det = m.a * m.d - m.b * m.c;
      if (Math.abs(det) > 1e-10) {
          this.cachedInvParentMatrix = {
              a: m.d / det, b: -m.b / det,
              c: -m.c / det, d: m.a / det,
              tx: (m.c * m.ty - m.d * m.tx) / det,
              ty: (m.b * m.tx - m.a * m.ty) / det,
          };
      }
  }
  ```
- `updateDrag(screenX, screenY)` — replace `this.coords.getScaleFactor(this.hostScene)` with `this.vp!.scaleFactor`. Replace `this.coords.setDesignPosition(target, x, y, hostScene)` with `this.coords.setDesignPosition(target, x, y, this.vp!, this.cachedInvParentMatrix)`.
- Update `snappingEngine.applySnapping(...)` call: pass `this.vp!` instead of `this.hostScene`.
- `endDrag()`: set `this.vp = null`, `this.cachedInvParentMatrix = null`.
- In `draw()`: the fallback calls `this.coords.getWorldPosition(obj)` — this needs `vp`. Either pass `vp` to `draw()` or keep using selectionMgr.getScreenBounds() (preferred, already works). Actually, the fallback in draw uses `getWorldPosition` which is now `getScreenPosition` and needs vp. Add a `vp: ViewportState` parameter to `draw()`. GizmoManager must pass it.
- Import ViewportState.

**RotateGizmo.ts (COORD-03):**
- Replace `private hostScene: Phaser.Scene | null` with `private vp: ViewportState | null`
- `startDrag(handle, screenX, screenY, target, vp: ViewportState)` — store `this.vp = vp`
- RotateGizmo doesn't call any CoordinateSystem methods during updateDrag() (it uses screen-space angle math directly), so the `vp` field is stored but not used during drag. That's fine — it's frozen at drag start per COORD-03.
- `endDrag()`: set `this.vp = null`
- **CRITICAL — draw() and computeRingRadius() updates:** `draw()` (line 67) calls `this.coords.getWorldPosition(obj)` every frame to get the gizmo center. `computeRingRadius()` (line 208) also calls `this.coords.getWorldPosition(obj)`. After Plan 02-01, `getWorldPosition` no longer exists (replaced by `getScreenPosition` which requires `vp`). Fix:
  - Add `vp: ViewportState` parameter to `draw()`: `draw(gfx, obj, selectionMgr, vp: ViewportState)`
  - In `draw()` line 67: replace `this.coords.getWorldPosition(obj)` with `this.coords.getScreenPosition(obj, vp)`
  - Add `vp: ViewportState` parameter to `computeRingRadius()`: `computeRingRadius(obj, selectionMgr, vp: ViewportState)`
  - In `computeRingRadius()` line 208: replace `this.coords.getWorldPosition(obj)` with `this.coords.getScreenPosition(obj, vp)`
  - Update the `draw()` call to `computeRingRadius()` to pass `vp`: `this.computeRingRadius(obj, selectionMgr, vp)`
- Import ViewportState.

**ScaleGizmo.ts (COORD-03):**
- Same pattern as RotateGizmo: replace `hostScene` with `vp: ViewportState`
- `startDrag(handle, screenX, screenY, target, vp: ViewportState)` — store `this.vp = vp`
- ScaleGizmo also operates in screen-space during drag, so vp is stored but may not be actively called.
- `endDrag()`: set `this.vp = null`
- Import ViewportState.

**HitAreaGizmo.ts (COORD-03 + COORD-04):**
- Replace `private hostScene: Phaser.Scene | null` with `private vp: ViewportState | null`
- `startDrag(handle, screenX, screenY, target, vp: ViewportState)` — store `this.vp = vp`
- Replace `getTransformHelpers()` method: instead of computing toScreen/screenDeltaToLocal inline, delegate to `this.coords.getHitAreaToScreen(obj)` and `this.coords.getHitAreaScreenDeltaToLocal(obj)`. The method can either be removed (callers use CoordinateSystem directly) or become a thin wrapper.
- Replace all calls to `getTransformHelpers(obj)` with:
  ```
  const toScreen = this.coords.getHitAreaToScreen(obj);
  const screenDeltaToLocal = this.coords.getHitAreaScreenDeltaToLocal(obj);
  ```
- `endDrag()`: set `this.vp = null`
- Import ViewportState.

**GizmoManager draw() update:**
- Change signature: `draw(gfx: Phaser.GameObjects.Graphics, vp: ViewportState): void`
- EditorScene calls `this.gizmoMgr.draw(this.gfx, vp)` where `vp` is the per-frame snapshot.
- Pass `vp` to ALL gizmo draw calls — each gizmo's `draw()` now requires it:
  - `this.moveGizmo.draw(gfx, selected, selectionMgr, vp)` — MoveGizmo.draw() needs vp for its fallback `getScreenPosition` call
  - `this.rotateGizmo.draw(gfx, selected, selectionMgr, vp)` — RotateGizmo.draw() needs vp for `getScreenPosition` in draw() and computeRingRadius()
  - `this.scaleGizmo.draw(gfx, selected, selectionMgr)` — ScaleGizmo.draw() does NOT need vp (uses only selectionMgr.getScreenBounds, no CoordinateSystem calls)
  - `this.hitAreaGizmo.draw(gfx, selected, selectionMgr)` — HitAreaGizmo.draw() does NOT need vp (uses matrix directly)
- Import ViewportState.
  </action>
  <verify>
`npx tsc --noEmit` — zero TypeScript errors across the entire project. Grep entire `src/` directory for `Phaser.Scene` passed to any CoordinateSystem method — should find zero occurrences. Grep for `getWorldPosition` — should find zero calls (replaced by `getScreenPosition`). Grep for `getScaleFactor.*scene\|getOffset.*scene` — should find zero calls.
  </verify>
  <done>
All 4 gizmos store ViewportState at drag start. MoveGizmo has cachedInvParentMatrix computed once at drag start. RotateGizmo.draw() and computeRingRadius() use getScreenPosition(obj, vp) instead of removed getWorldPosition(obj). HitAreaGizmo's getTransformHelpers replaced with CoordinateSystem.getHitAreaToScreen/getHitAreaScreenDeltaToLocal calls. GizmoManager passes ViewportState to startDrag and to draw() for gizmos that need it. Zero TypeScript errors. No Phaser.Scene parameter passed to CoordinateSystem anywhere in the codebase.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` succeeds with zero errors
- `grep -r "getWorldPosition" src/` returns zero results (method renamed to getScreenPosition)
- `grep -r "getScaleFactor\|getOffset" src/core/CoordinateSystem.ts` returns zero results (methods removed)
- `grep -rn "hostScene.*Phaser\.Scene" src/gizmos/ src/ui/InspectorPanel.ts` returns zero results (replaced with ViewportState)
- `grep -rn "getTransformHelpers" src/gizmos/HitAreaGizmo.ts` returns zero results OR only a thin wrapper
- `grep -rn "cachedInvParentMatrix" src/gizmos/MoveGizmo.ts` returns results confirming the cache exists
- `grep -rn "captureViewport" src/EditorScene.ts` confirms per-frame capture in update()
</verification>

<success_criteria>
- EditorScene captures ViewportState once in update(), distributes to all subsystems
- All gizmo startDrag() methods accept ViewportState, not Phaser.Scene
- MoveGizmo.cachedInvParentMatrix is set at startDrag, used in updateDrag, cleared at endDrag
- Three hit-area transform duplicates (EditorScene, SelectionManager, HitAreaGizmo) replaced with CoordinateSystem helper calls
- Full project compiles with zero TypeScript errors
- No CoordinateSystem method accepts Phaser.Scene anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/02-coordinate-system-refactor/02-02-SUMMARY.md`
</output>
